apiVersion: v1
kind: Service
metadata:
  name: redis-leader
  labels:
    app: redis
    role: leader
spec:
  selector:
    app: redis
    role: leader
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379